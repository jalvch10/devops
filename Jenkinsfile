pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                // This is where you can define your build steps
                sh 'echo "Building the project"'
            }
        }
        
        stage('Test') {
            steps {
                // This is where you can define your test steps
                sh 'echo "Running tests"'
            }
        }
        
        stage('Deploy') {
            steps {
                // This is where you can define your deployment steps
                sh 'echo "Deploying the application"'
            }
        }
    }
}
